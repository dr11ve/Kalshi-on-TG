TELEGRAM_TOKEN=YOUR_TELEGRAM_BOT_TOKEN

# Leave unset so the bot auto-picks a healthy host.
# KALSHI_BASE_URL=https://api.kalshi.com/trade-api/v2

# Poll cadence for trades (seconds)
POLL_INTERVAL=10

# Default whale threshold (USD)
DEFAULT_THRESH=5000
